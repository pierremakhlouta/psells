# Development dependencies: everything needed to run PSells, plus the tools used
# to test and maintain it. This is the file to install when working on the
# project. A deployment installs requirements.txt instead.
-r requirements.txt

pytest==9.1.1

# FastAPI's TestClient is built on this, so testing the API needs it even
# though nothing imports it directly. httpx2 rather than httpx: Starlette warns
# on every run that the older one is deprecated for the test client, and the
# suite passes with httpx removed entirely.
httpx2==2.13.0

# Required only by import_excel.py, the one-time migration out of Excel. Kept
# so that script can still be read and run against an old workbook.
openpyxl==3.1.5
# Lets tests/test_container.py read compose.yaml as data, so the rules it holds
# (ports on 127.0.0.1 only, no database port, no password in the file) are
# checked on every run instead of remembered.
pyyaml==6.0.3

